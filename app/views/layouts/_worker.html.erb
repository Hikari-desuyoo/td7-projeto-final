<div class="navbar-nav dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <%= current_worker.get_full_name %>
    </a>
    <div class="dropdown-menu" aria-labelledby="profileDropdown">
        <% if current_worker.complete_profile? %>
            <div class="nav-item">
                <%= 
                link_to t('layout.application.my_profile'),  
                current_worker, 
                :id => 'worker_profile_button',
                class: 'dropdown-item'
                %>
            </div>
            <div class="nav-item">
                <%= 
                link_to t('layout.application.my_project_applications_button'),  
                my_project_applications_project_applications_path, 
                :id => 'my_project_applications_button',
                class: 'dropdown-item'
                %>
            </div>
        <% end %>
        <div class="nav-item">
            <%= 
            link_to t('layout.application.logout_link'),  
            destroy_worker_session_path, 
            method: :delete, 
            :id => 'logout_link',
            class: 'dropdown-item'
            %>
        </div>
    </div>
</div>

